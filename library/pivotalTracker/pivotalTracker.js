class PivotalTracker {
    constructor(token, projectId){
        this.token = token;
        this.projectId = projectId;
    }
    getBlockersOfStory(storyId){
        // call the fetch endpoint
        console.log('Calling the fetch endpoint');
    }
}